%YAML 1.2
---
version: 1

id: altera

title: Altera's SoCFPGA platform device tree bindings

maintainer:
    -   name: Dinh Nguyen <dinguyen@opensource.altera.com>

description: >
    TODO

inherits:
    -   id: %ids%

properties:
    -   name: "compatible"
        constraint: |
            "ebv,socrates" && "altr,socfpga-cyclone5" && "altr,socfpga"
            "altr,socfpga-vt" && "altr,socfpga"
            "altr,socfpga-cyclone5" && "altr,socfpga"
            "altr,socfpga-arria5" && "altr,socfpga"
            "altr,socfpga-arria10" && "altr,socfpga"

    #address-cells:
    model:
    #size-cells:

...

# Altera's SoCFPGA platform device tree bindings
# ---------------------------------------------
#
# Boards with Cyclone 5 SoC:
# Required root node properties:
# compatible = "altr,socfpga-cyclone5", "altr,socfpga";
#
# Boards with Arria 5 SoC:
# Required root node properties:
# compatible = "altr,socfpga-arria5", "altr,socfpga";
#
# Boards with Arria 10 SoC:
# Required root node properties:
# compatible = "altr,socfpga-arria10", "altr,socfpga";
