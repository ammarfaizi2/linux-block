%YAML 1.2
---
version: 1

id: mxc-nand

title: Freescale's mxc_nand

maintainer:
    -   name: Uwe Kleine-König <u.kleine-koenig@pengutronix.de>

description: >
    TODO

inherits:
    -   id: %ids%

properties:
    -   name: "compatible"
        constraint: |
            "fsl,imx27-nand"

    #address-cells:
    clocks:
    interrupts:
    nand-bus-width:
    nand-ecc-mode:
    nand-on-flash-bbt:
    pinctrl-0:
    pinctrl-names:
    reg:
    #size-cells:
    status:

...

# * Freescale's mxc_nand
#
# Required properties:
# - compatible: "fsl,imxXX-nand"
# - reg: address range of the nfc block
# - interrupts: irq to be used
# - nand-bus-width: see nand.txt
# - nand-ecc-mode: see nand.txt
# - nand-on-flash-bbt: see nand.txt
#
# Example:
#
# 	nand@d8000000 {
# 		compatible = "fsl,imx27-nand";
# 		reg = <0xd8000000 0x1000>;
# 		interrupts = <29>;
# 		nand-bus-width = <8>;
# 		nand-ecc-mode = "hw";
# 	};
