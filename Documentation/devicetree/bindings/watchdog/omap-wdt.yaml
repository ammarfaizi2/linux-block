%YAML 1.2
---
version: 1

id: omap-wdt

title: TI Watchdog Timer (WDT) Controller for OMAP

maintainer:
    -   name: Uwe Kleine-König <u.kleine-koenig@pengutronix.de>

description: >
    TODO

inherits:
    -   id: %ids%

properties:
    -   name: "compatible"
        constraint: |
            "ti,omap5-wdt" && "ti,omap3-wdt"
            "ti,omap4-wdt" && "ti,omap3-wdt"
            "ti,omap3-wdt"
            "ti,am4372-wdt" && "ti,omap3-wdt"

    interrupts:
    reg:
    status:
    ti,hwmods:

...

# TI Watchdog Timer (WDT) Controller for OMAP
#
# Required properties:
# - compatible : "ti,omap3-wdt" for OMAP3 or "ti,omap4-wdt" for OMAP4
# - ti,hwmods : Name of the hwmod associated to the WDT
#
# Optional properties:
# - timeout-sec : default watchdog timeout in seconds
#
# Examples:
#
# wdt2: wdt@4a314000 {
# 	compatible = "ti,omap4-wdt", "ti,omap3-wdt";
# 	ti,hwmods = "wd_timer2";
# };
