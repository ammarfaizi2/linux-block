%YAML 1.2
---
version: 1

id: pwm-fan

title: Bindings for a fan connected to the PWM lines

maintainer:
    -   name: Kamil Debski <k.debski@samsung.com>

description: >
    TODO

inherits:
    -   id: %ids%

properties:
    -   name: "compatible"
        constraint: |
            "pwm-fan"

    #cooling-cells:
    cooling-levels:
    cooling-max-state:
    cooling-min-state:
    pwms:

...

# Bindings for a fan connected to the PWM lines
#
# Required properties:
# - compatible	: "pwm-fan"
# - pwms		: the PWM that is used to control the PWM fan
#
# Example:
# 	pwm-fan {
# 		compatible = "pwm-fan";
# 		status = "okay";
# 		pwms = <&pwm 0 10000 0>;
# 	};
